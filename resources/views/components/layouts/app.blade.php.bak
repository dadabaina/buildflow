<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <title>{{ $title ?? 'BuildFlow' }}</title>
    @vite(['resources/css/app.scss', 'resources/js/app.js'])
    @stack('styles')
</head>
<body>
<div class="d-flex" x-data="{ sidebarOpen: localStorage.getItem('sidebarOpen') !== 'false' }"
     x-init="$watch('sidebarOpen', v => localStorage.setItem('sidebarOpen', v))">

    {{-- Sidebar --}}
    <nav class="sidebar d-flex flex-column flex-shrink-0"
         :class="{ 'collapsed': !sidebarOpen }">

        {{-- Brand --}}
        <div class="sidebar-brand d-flex align-items-center gap-2">
            <span class="fs-4">🏗️</span>
            <span class="sidebar-brand-text text-white fw-bold fs-5">BuildFlow</span>
        </div>

        {{-- Navigation --}}
        <div class="sidebar-nav flex-grow-1 overflow-y-auto py-2">

            <span class="nav-section nav-label">Principal</span>

            <a href="{{ route('dashboard') }}"
               class="nav-link {{ request()->routeIs('dashboard') ? 'active' : '' }}">
                <i class="bi bi-speedometer2"></i>
                <span class="nav-label ms-2">Tableau de bord</span>
            </a>

            <span class="nav-section nav-label">Contacts</span>

            <a href="{{ route('clients.index') }}"
               class="nav-link {{ request()->routeIs('clients.*') ? 'active' : '' }}">
                <i class="bi bi-people"></i>
                <span class="nav-label ms-2">Clients</span>
            </a>

            <a href="{{ route('suppliers.index') }}"
               class="nav-link {{ request()->routeIs('suppliers.*') ? 'active' : '' }}">
                <i class="bi bi-truck"></i>
                <span class="nav-label ms-2">Fournisseurs</span>
            </a>

            <a href="{{ route('employees.index') }}"
               class="nav-link {{ request()->routeIs('employees.*') ? 'active' : '' }}">
                <i class="bi bi-person-badge"></i>
                <span class="nav-label ms-2">Employés</span>
            </a>

            <span class="nav-section nav-label">Chantiers</span>

            <a href="{{ route('projects.index') }}"
               class="nav-link {{ request()->routeIs('projects.*') ? 'active' : '' }}">
                <i class="bi bi-building"></i>
                <span class="nav-label ms-2">Chantiers</span>
            </a>

            <a href="{{ route('expenses.index') }}"
               class="nav-link {{ request()->routeIs('expenses.*') ? 'active' : '' }}">
                <i class="bi bi-receipt"></i>
                <span class="nav-label ms-2">Dépenses</span>
            </a>

            <a href="{{ route('purchase-orders.index') }}"
               class="nav-link {{ request()->routeIs('purchase-orders.*') ? 'active' : '' }}">
                <i class="bi bi-file-earmark-check"></i>
                <span class="nav-label ms-2">Bons de commande</span>
            </a>

            <a href="{{ route('tasks.index') }}"
               class="nav-link {{ request()->routeIs('tasks.*') ? 'active' : '' }}">
                <i class="bi bi-kanban"></i>
                <span class="nav-label ms-2">Tâches</span>
            </a>

            <a href="{{ route('attendances.index') }}"
               class="nav-link {{ request()->routeIs('attendances.*') ? 'active' : '' }}">
                <i class="bi bi-clock-history"></i>
                <span class="nav-label ms-2">Pointage</span>
            </a>

            @can('site_reports.view')
            <a href="{{ route('site-reports.index') }}"
               class="nav-link {{ request()->routeIs('site-reports.*') ? 'active' : '' }}">
                <i class="bi bi-journal-text"></i>
                <span class="nav-label ms-2">Comptes-rendus</span>
            </a>
            @endcan

            @can('reception_reports.view')
            <a href="{{ route('reception-reports.index') }}"
               class="nav-link {{ request()->routeIs('reception-reports.*') ? 'active' : '' }}">
                <i class="bi bi-clipboard2-check"></i>
                <span class="nav-label ms-2">PV Réception</span>
            </a>
            @endcan

            <span class="nav-section nav-label">Commercial</span>

            <a href="{{ route('quotes.index') }}"
               class="nav-link {{ request()->routeIs('quotes.*') ? 'active' : '' }}">
                <i class="bi bi-file-earmark-text"></i>
                <span class="nav-label ms-2">Devis</span>
            </a>

            <a href="{{ route('amendments.index') }}"
               class="nav-link {{ request()->routeIs('amendments.*') ? 'active' : '' }}">
                <i class="bi bi-file-earmark-plus"></i>
                <span class="nav-label ms-2">Avenants</span>
            </a>

            <a href="{{ route('progress-billings.index') }}"
               class="nav-link {{ request()->routeIs('progress-billings.*') ? 'active' : '' }}">
                <i class="bi bi-bar-chart-steps"></i>
                <span class="nav-label ms-2">Situations</span>
            </a>

            <a href="{{ route('invoices.index') }}"
               class="nav-link {{ request()->routeIs('invoices.*') ? 'active' : '' }}">
                <i class="bi bi-file-earmark-richtext"></i>
                <span class="nav-label ms-2">Factures</span>
            </a>

            <a href="{{ route('payments.index') }}"
               class="nav-link {{ request()->routeIs('payments.*') ? 'active' : '' }}">
                <i class="bi bi-cash-coin"></i>
                <span class="nav-label ms-2">Paiements</span>
            </a>

            <span class="nav-section nav-label">Ressources</span>

            <a href="{{ route('documents.index') }}"
               class="nav-link {{ request()->routeIs('documents.*') ? 'active' : '' }}">
                <i class="bi bi-folder2-open"></i>
                <span class="nav-label ms-2">Documents</span>
            </a>

            <a href="{{ route('materials.index') }}"
               class="nav-link {{ request()->routeIs('materials.*') ? 'active' : '' }}">
                <i class="bi bi-box-seam"></i>
                <span class="nav-label ms-2">Matériaux</span>
            </a>

            <a href="{{ route('dosage.index') }}"
               class="nav-link {{ request()->routeIs('dosage.*') ? 'active' : '' }}">
                <i class="bi bi-calculator"></i>
                <span class="nav-label ms-2">Dosages DBE</span>
            </a>

            @can('equipments.view')
            <a href="{{ route('equipments.index') }}"
               class="nav-link {{ request()->routeIs('equipments.*') ? 'active' : '' }}">
                <i class="bi bi-tools"></i>
                <span class="nav-label ms-2">Matériels</span>
            </a>
            @endcan

            @can('stock.view')
            <a href="{{ route('warehouses.index') }}"
               class="nav-link {{ request()->routeIs('warehouses.*') ? 'active' : '' }}">
                <i class="bi bi-archive"></i>
                <span class="nav-label ms-2">Dépôts</span>
            </a>
            <a href="{{ route('stock.dashboard') }}"
               class="nav-link {{ request()->routeIs('stock.*') ? 'active' : '' }}">
                <i class="bi bi-boxes"></i>
                <span class="nav-label ms-2">Stock</span>
            </a>
            @endcan

            <span class="nav-section nav-label">Rapports</span>

            <a href="{{ route('reports.index') }}"
               class="nav-link {{ request()->routeIs('reports.*') ? 'active' : '' }}">
                <i class="bi bi-graph-up"></i>
                <span class="nav-label ms-2">Rapports</span>
            </a>

            @can('settings.view')
            <span class="nav-section nav-label">Administration</span>

            {{-- Paramètres sous-menu --}}
            <div x-data="{ settingsOpen: {{ request()->routeIs('settings.*') || request()->routeIs('expense-categories.*') ? 'true' : 'false' }} }">
                <button type="button"
                        class="nav-link w-100 text-start border-0 bg-transparent {{ request()->routeIs('settings.*') || request()->routeIs('expense-categories.*') ? 'active' : '' }}"
                        @click="settingsOpen = !settingsOpen">
                    <i class="bi bi-gear"></i>
                    <span class="nav-label ms-2 d-flex align-items-center justify-content-between">
                        Paramètres
                        <i class="bi nav-label ms-auto" :class="settingsOpen ? 'bi-chevron-up' : 'bi-chevron-down'" style="font-size:.7rem"></i>
                    </span>
                </button>
                <div x-show="settingsOpen && sidebarOpen" style="display:none">
                    <a href="{{ route('settings.index') }}#entreprise"
                       class="nav-link nav-sub {{ request()->routeIs('settings.*') ? 'active' : '' }}">
                        <i class="bi bi-building"></i>
                        <span class="nav-label ms-2">Entreprise</span>
                    </a>
                    <a href="{{ route('settings.index') }}#regions"
                       class="nav-link nav-sub">
                        <i class="bi bi-geo-alt"></i>
                        <span class="nav-label ms-2">Régions</span>
                    </a>
                    <a href="{{ route('settings.index') }}#metiers"
                       class="nav-link nav-sub">
                        <i class="bi bi-briefcase"></i>
                        <span class="nav-label ms-2">Postes / Fonctions</span>
                    </a>
                    <a href="{{ route('settings.index') }}#unites"
                       class="nav-link nav-sub">
                        <i class="bi bi-rulers"></i>
                        <span class="nav-label ms-2">Types d'unités</span>
                    </a>
                    <a href="{{ route('settings.index') }}#categories"
                       class="nav-link nav-sub">
                        <i class="bi bi-tags"></i>
                        <span class="nav-label ms-2">Catégories dépenses</span>
                    </a>
                    <a href="{{ route('settings.index') }}#salaires"
                       class="nav-link nav-sub">
                        <i class="bi bi-currency-dollar"></i>
                        <span class="nav-label ms-2">Grille salariale</span>
                    </a>
                    <a href="{{ route('settings.index') }}#mat_categories"
                       class="nav-link nav-sub">
                        <i class="bi bi-boxes"></i>
                        <span class="nav-label ms-2">Catégories matériaux</span>
                    </a>
                </div>
            </div>

            <a href="{{ route('users.index') }}"
               class="nav-link {{ request()->routeIs('users.*') ? 'active' : '' }}">
                <i class="bi bi-person-lock"></i>
                <span class="nav-label ms-2">Utilisateurs</span>
            </a>
            @endcan
        </div>

        {{-- Toggle --}}
        <div class="p-2 border-top border-white-10">
            <button class="btn btn-sm w-100 text-white-50"
                    @click="sidebarOpen = !sidebarOpen">
                <i class="bi" :class="sidebarOpen ? 'bi-chevron-double-left' : 'bi-chevron-double-right'"></i>
                <span class="nav-label ms-1">Réduire</span>
            </button>
        </div>
    </nav>

    {{-- Main area --}}
    <div class="flex-grow-1 d-flex flex-column overflow-hidden">

        {{-- Topbar --}}
        <header class="topbar d-flex align-items-center px-4 gap-3">
            {{-- Breadcrumb slot --}}
            <div class="flex-grow-1">
                {{ $breadcrumb ?? '' }}
            </div>

            {{-- Notifications --}}
            <div class="dropdown">
                <button class="btn btn-sm btn-light position-relative" data-bs-toggle="dropdown">
                    <i class="bi bi-bell"></i>
                    @php $unreadCount = auth()->user()?->unreadNotifications()->count(); @endphp
                    @if($unreadCount > 0)
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size:.6rem">
                        {{ $unreadCount > 9 ? '9+' : $unreadCount }}
                    </span>
                    @endif
                </button>
                <div class="dropdown-menu dropdown-menu-end p-0" style="width:340px;max-height:400px;overflow-y:auto;">
                    <div class="d-flex justify-content-between align-items-center p-3 border-bottom">
                        <span class="fw-semibold small">Notifications</span>
                        <a href="{{ route('notifications.index') }}" class="small text-primary">Voir tout</a>
                    </div>
                    @forelse(auth()->user()?->notifications()->latest()->take(5)->get() ?? [] as $notif)
                    @php
                        $nd = is_array($notif->data) ? $notif->data : json_decode($notif->data, true);
                    @endphp
                    <a href="{{ route('notifications.read', $notif->id) }}"
                       class="dropdown-item d-flex gap-2 align-items-start p-3 {{ is_null($notif->read_at) ? 'bg-primary-subtle bg-opacity-10' : '' }}">
                        <i class="bi {{ $nd['icon'] ?? 'bi-bell' }} text-{{ $nd['color'] ?? 'primary' }} mt-1"></i>
                        <div>
                            <p class="mb-0 small fw-semibold">{{ $nd['title'] ?? '' }}</p>
                            <p class="mb-0 text-muted" style="font-size:.75rem">{{ Str::limit($nd['message'] ?? '', 60) }}</p>
                            <p class="mb-0 text-muted" style="font-size:.7rem">{{ $notif->created_at->diffForHumans() }}</p>
                        </div>
                    </a>
                    @empty
                    <div class="text-center text-muted py-4 small">
                        <i class="bi bi-bell-slash d-block mb-1 fs-4 opacity-25"></i>Aucune notification
                    </div>
                    @endforelse
                </div>
            </div>

            {{-- User menu --}}
            <div class="dropdown">
                <button class="btn btn-sm btn-light dropdown-toggle d-flex align-items-center gap-2"
                        data-bs-toggle="dropdown">
                    <img src="{{ auth()->user()->avatar_url }}"
                         class="rounded-circle" width="28" height="28"
                         alt="{{ auth()->user()->name }}">
                    <span class="d-none d-md-inline">{{ auth()->user()->name }}</span>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><h6 class="dropdown-header">{{ auth()->user()->company?->name }}</h6></li>
                    <li><a class="dropdown-item" href="{{ route('profile.edit') }}">
                        <i class="bi bi-person me-2"></i> Mon profil
                    </a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <form method="POST" action="{{ route('logout') }}">
                            @csrf
                            <button type="submit" class="dropdown-item text-danger">
                                <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                            </button>
                        </form>
                    </li>
                </ul>
            </div>
        </header>

        {{-- Page content --}}
        <main class="main-content flex-grow-1 overflow-y-auto p-4">
            {{-- Flash messages --}}
            @if(session('success'))
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="bi bi-check-circle me-2"></i>{{ session('success') }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            @endif
            @if(session('error'))
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="bi bi-exclamation-circle me-2"></i>{{ session('error') }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
            @endif

            {{ $slot }}
        </main>
    </div>
</div>

@stack('scripts')
</body>
</html>
